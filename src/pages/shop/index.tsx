import { memo } from "react";
import ShopView from "../../components/shop-view";

const Shop = () => {
  return (
    <div className="">
      <ShopView />
    </div>
  );
};

export default memo(Shop);
